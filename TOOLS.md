# TOOLS.md

Tool and skill definitions live in:

- `packages/heiwa_skills/` for skill plugins
- `packages/heiwa_sdk/heiwa_sdk/claw.py` for the HeiwaClaw LLM gateway
- `config/swarm/ai_router.json` for model/compute routing config
