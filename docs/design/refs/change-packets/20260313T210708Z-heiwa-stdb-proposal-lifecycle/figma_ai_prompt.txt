Update the Heiwa architecture diagram to show the March 13 proposal-lifecycle migration.

Visual requirements:
- Keep the three physical homes: Railway, MacBook, WSL/Ubuntu.
- Keep SpacetimeDB as the center of state authority.
- Add a distinct proposal authority cluster inside SpacetimeDB with five records:
  `proposals`, `proposal_consents`, `approval_requests`, `approval_decisions`, `capability_leases`.
- Show the hub HTTP surface (`/proposals`, `/proposals/claim`, `/proposals/{id}/consent`, heartbeat) writing into that STDB cluster.
- Show `tick.py` as a scheduler that computes assignments and writes them into STDB, but label it “transitional scheduler” rather than state authority.
- Show node claim flow as:
  assignment in STDB -> node claim -> capability lease issued -> execution path.
- Keep `approval.py` and Discord / RFC surfaces visible but visually downgraded as compatibility / notification layers.
- Keep websocket status stream visible, but do not imply proposal subscriptions exist yet.

Messaging requirements:
- Emphasize “STDB authoritative, scheduler transitional”.
- Emphasize “lease issued before execution”.
- Emphasize “public API stable, backend authority moved”.
