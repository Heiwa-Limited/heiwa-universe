# Design Drift Summary

- Add a **Context Map** layer above the runtime architecture.
- Show `HEIWA.md` as the cold-start routing table.
- Show `rooms/` as explicit context silos, not just generic documentation.
- Show `SOUL.md` as a compatibility shim that redirects to the new map.
- Keep the runtime boxes unchanged beneath this new repo-readable guidance layer.
