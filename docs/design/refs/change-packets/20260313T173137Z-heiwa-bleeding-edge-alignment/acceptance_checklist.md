# Acceptance Checklist

- The roadmap overlay clearly distinguishes `live` vs `proposed`.
- The fast path remains Broker -> HeiwaClaw -> MCP / Adapters -> SpacetimeDB / WebSockets.
- `HeiwaMemory` is shown as STDB-first, not filesystem-first.
- `HeiwaBench` is visually distinct as an eval plane and release gate.
- `HeiwaCells` is shown as the packaging/productization layer over narrow agents.
- `HeiwaPulse` is clearly optional and async.
- `HeiwaSafehouse` is shown as containment around execution.
- `HeiwaLab` is visually isolated from the production path.
