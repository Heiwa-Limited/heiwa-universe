Refresh the Heiwa public architecture/design file so it matches the current repo truth alignment.

Visual direction:
- crisp, technical, fast
- no generic “AI enterprise mesh” language
- emphasize a narrow public/runtime split
- show speed architecture with WebSockets and SpacetimeDB, not slow compatibility layers

Ground truth:
- Canonical repo: /Users/dmcgregsauce/heiwa
- Supported first-class surfaces: CLI, MCP, HTTP API, docs
- Runtime host: Railway
- Public shell host: Cloudflare Pages
- State direction: SpacetimeDB
- Live transport: WebSocket-first, HTTP fallback only for diagnostics

Remove or de-emphasize:
- heiwa-limited as an active target
- Discord as a required surface
- auth.heiwa.ltd as a current public surface
- experimental canvas as a supported product surface
- placeholder Codex/OpenClaw personas as productized capabilities
- older “enterprise mesh / NATS / PostgreSQL sovereignty” marketing framing

Show these zones clearly:
1. Operator surfaces: CLI, MCP clients
2. Runtime plane: Railway hub, HTTP API, MCP ingress
3. State plane: SpacetimeDB
4. Public shell: Cloudflare Pages marketing, docs, read-only status
5. CI enforcement: hub smoke tests, docs build, static public-shell checks

Use the supplied `architecture_nodes.json` and `architecture_connections.json` as the source data for the updated graph. Preserve human-readable labels and a clean inspector-friendly layout.
