# Acceptance Checklist

- `HeiwaCells` is shown as live but seed-stage.
- `HeiwaBench` is shown as live but seed-stage.
- CLI and MCP both visibly connect to these surfaces.
- CI visibly gates on `HeiwaBench`.
- Broker -> HeiwaClaw -> MCP/Adapters -> SpacetimeDB/WebSockets remains the core fast path.
- The diagram does not overstate maturity beyond the current implementation.
