# Acceptance Checklist

- `HEIWA.md` is visible as the root context map.
- `SOUL.md` is shown as a compatibility shim, not a second competing manual.
- `rooms/` is shown as the context silo layer.
- The runtime backbone remains visible and unchanged underneath the context layer.
- The diagram does not confuse repo-readable context with live runtime state.
