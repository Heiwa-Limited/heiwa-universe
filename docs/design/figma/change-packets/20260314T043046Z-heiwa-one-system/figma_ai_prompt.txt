Update the Heiwa architecture diagram to match the current Railway-first control plane and OAuth routing topology.

Use these invariants:
- Railway is the always-on brain.
- SpacetimeDB is the durable state layer.
- NATS is ephemeral transport only, not durable storage.
- MacBook Node A and WSL/PC Node B are modular, ephemeral executors.
- Railway plan annotation should read: 32 GB RAM, 32 vCPU, 100 GB shared disk, $20 USD/month.

Render the system in three bands:
1. Operator surfaces
   - Operator
   - Heiwa CLI
2. Persistent control plane on Railway
   - heiwa-cloud-hq
   - NATS
   - SpacetimeDB
   - heiwa-scheduler
3. Execution lanes
   - Local Ollama on Node A / Node B
   - Gemini CLI OAuth lane for research
   - Antigravity OAuth lane for strategy, shown as isolated OpenClaw profile
   - Claude Code OAuth lane for review
   - Codex OAuth lane for build implementation

Emphasize the routing rule text:
- cheapest acceptable route first
- privacy-first
- local-first before remote
- subscription OAuth lanes before paid API overflow

Remove any implication that:
- local laptops run permanent mini-servers
- Antigravity shares the same rate group as Gemini CLI
- premium cloud routing is a single undifferentiated model bucket
- the default bare `heiwa` surface opens an always-on TUI

Show directed edges:
- Operator -> Heiwa CLI
- Heiwa CLI -> heiwa-cloud-hq
- heiwa-cloud-hq -> NATS
- heiwa-cloud-hq -> SpacetimeDB
- heiwa-cloud-hq -> heiwa-scheduler
- heiwa-cloud-hq -> Node A / Node B
- heiwa-cloud-hq -> each OAuth lane
- Node A / Node B -> Local Ollama
