[service]
name = "heiwa-core"

[build]
builder = "DOCKERFILE"
dockerfilePath = "apps/heiwa_core/Dockerfile"

[deploy]
startCommand = "bash apps/heiwa_core/start.sh"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
healthcheckPath = "/health"
healthcheckTimeout = 30
