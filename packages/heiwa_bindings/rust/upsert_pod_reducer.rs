// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct UpsertPodArgs {
    pub pod_id: String,
    pub host_identity: String,
    pub provider: String,
    pub runtime_capabilities: Vec<String>,
    pub trust_tier: String,
    pub privacy_floor: String,
    pub gpu_inventory: Vec<String>,
    pub liveness: String,
    pub leaseable: bool,
    pub registered_at: String,
    pub last_heartbeat: String,
}

impl From<UpsertPodArgs> for super::Reducer {
    fn from(args: UpsertPodArgs) -> Self {
        Self::UpsertPod {
            pod_id: args.pod_id,
            host_identity: args.host_identity,
            provider: args.provider,
            runtime_capabilities: args.runtime_capabilities,
            trust_tier: args.trust_tier,
            privacy_floor: args.privacy_floor,
            gpu_inventory: args.gpu_inventory,
            liveness: args.liveness,
            leaseable: args.leaseable,
            registered_at: args.registered_at,
            last_heartbeat: args.last_heartbeat,
        }
    }
}

impl __sdk::InModule for UpsertPodArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `upsert_pod`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait upsert_pod {
    /// Request that the remote module invoke the reducer `upsert_pod` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`upsert_pod:upsert_pod_then`] to run a callback after the reducer completes.
    fn upsert_pod(
        &self,
        pod_id: String,
        host_identity: String,
        provider: String,
        runtime_capabilities: Vec<String>,
        trust_tier: String,
        privacy_floor: String,
        gpu_inventory: Vec<String>,
        liveness: String,
        leaseable: bool,
        registered_at: String,
        last_heartbeat: String,
    ) -> __sdk::Result<()> {
        self.upsert_pod_then(
            pod_id,
            host_identity,
            provider,
            runtime_capabilities,
            trust_tier,
            privacy_floor,
            gpu_inventory,
            liveness,
            leaseable,
            registered_at,
            last_heartbeat,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `upsert_pod` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn upsert_pod_then(
        &self,
        pod_id: String,
        host_identity: String,
        provider: String,
        runtime_capabilities: Vec<String>,
        trust_tier: String,
        privacy_floor: String,
        gpu_inventory: Vec<String>,
        liveness: String,
        leaseable: bool,
        registered_at: String,
        last_heartbeat: String,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl upsert_pod for super::RemoteReducers {
    fn upsert_pod_then(
        &self,
        pod_id: String,
        host_identity: String,
        provider: String,
        runtime_capabilities: Vec<String>,
        trust_tier: String,
        privacy_floor: String,
        gpu_inventory: Vec<String>,
        liveness: String,
        leaseable: bool,
        registered_at: String,
        last_heartbeat: String,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            UpsertPodArgs {
                pod_id,
                host_identity,
                provider,
                runtime_capabilities,
                trust_tier,
                privacy_floor,
                gpu_inventory,
                liveness,
                leaseable,
                registered_at,
                last_heartbeat,
            },
            callback,
        )
    }
}
