// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct UpsertSessionArgs {
    pub session_id: String,
    pub node_id: String,
    pub instance_id: String,
    pub runtime: String,
    pub runtime_version: String,
    pub worker_version: String,
    pub protocol: String,
    pub capabilities_json: String,
    pub metadata_json: String,
    pub max_concurrency: i64,
    pub active_tasks: u32,
    pub status: String,
    pub load: f64,
    pub created_at: String,
    pub updated_at: String,
    pub expires_at: String,
    pub last_seen_at: String,
    pub closed_at: Option<String>,
    pub current_task_id: Option<String>,
    pub lease_id: Option<String>,
}

impl From<UpsertSessionArgs> for super::Reducer {
    fn from(args: UpsertSessionArgs) -> Self {
        Self::UpsertSession {
            session_id: args.session_id,
            node_id: args.node_id,
            instance_id: args.instance_id,
            runtime: args.runtime,
            runtime_version: args.runtime_version,
            worker_version: args.worker_version,
            protocol: args.protocol,
            capabilities_json: args.capabilities_json,
            metadata_json: args.metadata_json,
            max_concurrency: args.max_concurrency,
            active_tasks: args.active_tasks,
            status: args.status,
            load: args.load,
            created_at: args.created_at,
            updated_at: args.updated_at,
            expires_at: args.expires_at,
            last_seen_at: args.last_seen_at,
            closed_at: args.closed_at,
            current_task_id: args.current_task_id,
            lease_id: args.lease_id,
        }
    }
}

impl __sdk::InModule for UpsertSessionArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `upsert_session`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait upsert_session {
    /// Request that the remote module invoke the reducer `upsert_session` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`upsert_session:upsert_session_then`] to run a callback after the reducer completes.
    fn upsert_session(
        &self,
        session_id: String,
        node_id: String,
        instance_id: String,
        runtime: String,
        runtime_version: String,
        worker_version: String,
        protocol: String,
        capabilities_json: String,
        metadata_json: String,
        max_concurrency: i64,
        active_tasks: u32,
        status: String,
        load: f64,
        created_at: String,
        updated_at: String,
        expires_at: String,
        last_seen_at: String,
        closed_at: Option<String>,
        current_task_id: Option<String>,
        lease_id: Option<String>,
    ) -> __sdk::Result<()> {
        self.upsert_session_then(
            session_id,
            node_id,
            instance_id,
            runtime,
            runtime_version,
            worker_version,
            protocol,
            capabilities_json,
            metadata_json,
            max_concurrency,
            active_tasks,
            status,
            load,
            created_at,
            updated_at,
            expires_at,
            last_seen_at,
            closed_at,
            current_task_id,
            lease_id,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `upsert_session` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn upsert_session_then(
        &self,
        session_id: String,
        node_id: String,
        instance_id: String,
        runtime: String,
        runtime_version: String,
        worker_version: String,
        protocol: String,
        capabilities_json: String,
        metadata_json: String,
        max_concurrency: i64,
        active_tasks: u32,
        status: String,
        load: f64,
        created_at: String,
        updated_at: String,
        expires_at: String,
        last_seen_at: String,
        closed_at: Option<String>,
        current_task_id: Option<String>,
        lease_id: Option<String>,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl upsert_session for super::RemoteReducers {
    fn upsert_session_then(
        &self,
        session_id: String,
        node_id: String,
        instance_id: String,
        runtime: String,
        runtime_version: String,
        worker_version: String,
        protocol: String,
        capabilities_json: String,
        metadata_json: String,
        max_concurrency: i64,
        active_tasks: u32,
        status: String,
        load: f64,
        created_at: String,
        updated_at: String,
        expires_at: String,
        last_seen_at: String,
        closed_at: Option<String>,
        current_task_id: Option<String>,
        lease_id: Option<String>,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            UpsertSessionArgs {
                session_id,
                node_id,
                instance_id,
                runtime,
                runtime_version,
                worker_version,
                protocol,
                capabilities_json,
                metadata_json,
                max_concurrency,
                active_tasks,
                status,
                load,
                created_at,
                updated_at,
                expires_at,
                last_seen_at,
                closed_at,
                current_task_id,
                lease_id,
            },
            callback,
        )
    }
}
