// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct RecordDrexDecisionArgs {
    pub decision_id: String,
    pub request_id: String,
    pub task_id: String,
    pub active_tier: String,
    pub route_runtime: String,
    pub route_model: String,
    pub scope: f64,
    pub abstraction: f64,
    pub context_span: f64,
    pub execution_proximity: f64,
    pub blast_radius: f64,
    pub coordination_load: f64,
    pub latency_pressure: f64,
    pub macro_score: f64,
    pub meso_score: f64,
    pub micro_score: f64,
    pub score_confidence: f64,
    pub authority_required: String,
    pub requires_approval: bool,
    pub reasons_json: String,
    pub vector_json: String,
    pub scorecard_json: String,
    pub gate_json: String,
    pub policy_version: String,
    pub created_at_ms: u64,
}

impl From<RecordDrexDecisionArgs> for super::Reducer {
    fn from(args: RecordDrexDecisionArgs) -> Self {
        Self::RecordDrexDecision {
            decision_id: args.decision_id,
            request_id: args.request_id,
            task_id: args.task_id,
            active_tier: args.active_tier,
            route_runtime: args.route_runtime,
            route_model: args.route_model,
            scope: args.scope,
            abstraction: args.abstraction,
            context_span: args.context_span,
            execution_proximity: args.execution_proximity,
            blast_radius: args.blast_radius,
            coordination_load: args.coordination_load,
            latency_pressure: args.latency_pressure,
            macro_score: args.macro_score,
            meso_score: args.meso_score,
            micro_score: args.micro_score,
            score_confidence: args.score_confidence,
            authority_required: args.authority_required,
            requires_approval: args.requires_approval,
            reasons_json: args.reasons_json,
            vector_json: args.vector_json,
            scorecard_json: args.scorecard_json,
            gate_json: args.gate_json,
            policy_version: args.policy_version,
            created_at_ms: args.created_at_ms,
        }
    }
}

impl __sdk::InModule for RecordDrexDecisionArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `record_drex_decision`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait record_drex_decision {
    /// Request that the remote module invoke the reducer `record_drex_decision` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`record_drex_decision:record_drex_decision_then`] to run a callback after the reducer completes.
    fn record_drex_decision(
        &self,
        decision_id: String,
        request_id: String,
        task_id: String,
        active_tier: String,
        route_runtime: String,
        route_model: String,
        scope: f64,
        abstraction: f64,
        context_span: f64,
        execution_proximity: f64,
        blast_radius: f64,
        coordination_load: f64,
        latency_pressure: f64,
        macro_score: f64,
        meso_score: f64,
        micro_score: f64,
        score_confidence: f64,
        authority_required: String,
        requires_approval: bool,
        reasons_json: String,
        vector_json: String,
        scorecard_json: String,
        gate_json: String,
        policy_version: String,
        created_at_ms: u64,
    ) -> __sdk::Result<()> {
        self.record_drex_decision_then(
            decision_id,
            request_id,
            task_id,
            active_tier,
            route_runtime,
            route_model,
            scope,
            abstraction,
            context_span,
            execution_proximity,
            blast_radius,
            coordination_load,
            latency_pressure,
            macro_score,
            meso_score,
            micro_score,
            score_confidence,
            authority_required,
            requires_approval,
            reasons_json,
            vector_json,
            scorecard_json,
            gate_json,
            policy_version,
            created_at_ms,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `record_drex_decision` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn record_drex_decision_then(
        &self,
        decision_id: String,
        request_id: String,
        task_id: String,
        active_tier: String,
        route_runtime: String,
        route_model: String,
        scope: f64,
        abstraction: f64,
        context_span: f64,
        execution_proximity: f64,
        blast_radius: f64,
        coordination_load: f64,
        latency_pressure: f64,
        macro_score: f64,
        meso_score: f64,
        micro_score: f64,
        score_confidence: f64,
        authority_required: String,
        requires_approval: bool,
        reasons_json: String,
        vector_json: String,
        scorecard_json: String,
        gate_json: String,
        policy_version: String,
        created_at_ms: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl record_drex_decision for super::RemoteReducers {
    fn record_drex_decision_then(
        &self,
        decision_id: String,
        request_id: String,
        task_id: String,
        active_tier: String,
        route_runtime: String,
        route_model: String,
        scope: f64,
        abstraction: f64,
        context_span: f64,
        execution_proximity: f64,
        blast_radius: f64,
        coordination_load: f64,
        latency_pressure: f64,
        macro_score: f64,
        meso_score: f64,
        micro_score: f64,
        score_confidence: f64,
        authority_required: String,
        requires_approval: bool,
        reasons_json: String,
        vector_json: String,
        scorecard_json: String,
        gate_json: String,
        policy_version: String,
        created_at_ms: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            RecordDrexDecisionArgs {
                decision_id,
                request_id,
                task_id,
                active_tier,
                route_runtime,
                route_model,
                scope,
                abstraction,
                context_span,
                execution_proximity,
                blast_radius,
                coordination_load,
                latency_pressure,
                macro_score,
                meso_score,
                micro_score,
                score_confidence,
                authority_required,
                requires_approval,
                reasons_json,
                vector_json,
                scorecard_json,
                gate_json,
                policy_version,
                created_at_ms,
            },
            callback,
        )
    }
}
