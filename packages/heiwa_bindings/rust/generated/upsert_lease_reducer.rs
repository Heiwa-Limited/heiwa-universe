// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct UpsertLeaseArgs {
    pub lease_id: String,
    pub task_id: String,
    pub session_id: String,
    pub node_id: String,
    pub capability: String,
    pub status: String,
    pub issued_at: String,
    pub updated_at: String,
    pub expires_at: String,
    pub acked_at: Option<String>,
    pub completed_at: Option<String>,
    pub failure_code: Option<String>,
    pub reason: Option<String>,
}

impl From<UpsertLeaseArgs> for super::Reducer {
    fn from(args: UpsertLeaseArgs) -> Self {
        Self::UpsertLease {
            lease_id: args.lease_id,
            task_id: args.task_id,
            session_id: args.session_id,
            node_id: args.node_id,
            capability: args.capability,
            status: args.status,
            issued_at: args.issued_at,
            updated_at: args.updated_at,
            expires_at: args.expires_at,
            acked_at: args.acked_at,
            completed_at: args.completed_at,
            failure_code: args.failure_code,
            reason: args.reason,
        }
    }
}

impl __sdk::InModule for UpsertLeaseArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `upsert_lease`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait upsert_lease {
    /// Request that the remote module invoke the reducer `upsert_lease` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`upsert_lease:upsert_lease_then`] to run a callback after the reducer completes.
    fn upsert_lease(
        &self,
        lease_id: String,
        task_id: String,
        session_id: String,
        node_id: String,
        capability: String,
        status: String,
        issued_at: String,
        updated_at: String,
        expires_at: String,
        acked_at: Option<String>,
        completed_at: Option<String>,
        failure_code: Option<String>,
        reason: Option<String>,
    ) -> __sdk::Result<()> {
        self.upsert_lease_then(
            lease_id,
            task_id,
            session_id,
            node_id,
            capability,
            status,
            issued_at,
            updated_at,
            expires_at,
            acked_at,
            completed_at,
            failure_code,
            reason,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `upsert_lease` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn upsert_lease_then(
        &self,
        lease_id: String,
        task_id: String,
        session_id: String,
        node_id: String,
        capability: String,
        status: String,
        issued_at: String,
        updated_at: String,
        expires_at: String,
        acked_at: Option<String>,
        completed_at: Option<String>,
        failure_code: Option<String>,
        reason: Option<String>,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl upsert_lease for super::RemoteReducers {
    fn upsert_lease_then(
        &self,
        lease_id: String,
        task_id: String,
        session_id: String,
        node_id: String,
        capability: String,
        status: String,
        issued_at: String,
        updated_at: String,
        expires_at: String,
        acked_at: Option<String>,
        completed_at: Option<String>,
        failure_code: Option<String>,
        reason: Option<String>,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            UpsertLeaseArgs {
                lease_id,
                task_id,
                session_id,
                node_id,
                capability,
                status,
                issued_at,
                updated_at,
                expires_at,
                acked_at,
                completed_at,
                failure_code,
                reason,
            },
            callback,
        )
    }
}
