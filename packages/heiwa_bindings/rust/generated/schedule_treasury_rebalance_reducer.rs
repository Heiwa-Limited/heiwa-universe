// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct ScheduleTreasuryRebalanceArgs {
    pub schedule_id: String,
    pub treasury_id: String,
    pub reason: String,
    pub priority: i64,
    pub scheduled_at: String,
}

impl From<ScheduleTreasuryRebalanceArgs> for super::Reducer {
    fn from(args: ScheduleTreasuryRebalanceArgs) -> Self {
        Self::ScheduleTreasuryRebalance {
            schedule_id: args.schedule_id,
            treasury_id: args.treasury_id,
            reason: args.reason,
            priority: args.priority,
            scheduled_at: args.scheduled_at,
        }
    }
}

impl __sdk::InModule for ScheduleTreasuryRebalanceArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `schedule_treasury_rebalance`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait schedule_treasury_rebalance {
    /// Request that the remote module invoke the reducer `schedule_treasury_rebalance` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`schedule_treasury_rebalance:schedule_treasury_rebalance_then`] to run a callback after the reducer completes.
    fn schedule_treasury_rebalance(
        &self,
        schedule_id: String,
        treasury_id: String,
        reason: String,
        priority: i64,
        scheduled_at: String,
    ) -> __sdk::Result<()> {
        self.schedule_treasury_rebalance_then(
            schedule_id,
            treasury_id,
            reason,
            priority,
            scheduled_at,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `schedule_treasury_rebalance` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn schedule_treasury_rebalance_then(
        &self,
        schedule_id: String,
        treasury_id: String,
        reason: String,
        priority: i64,
        scheduled_at: String,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl schedule_treasury_rebalance for super::RemoteReducers {
    fn schedule_treasury_rebalance_then(
        &self,
        schedule_id: String,
        treasury_id: String,
        reason: String,
        priority: i64,
        scheduled_at: String,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            ScheduleTreasuryRebalanceArgs {
                schedule_id,
                treasury_id,
                reason,
                priority,
                scheduled_at,
            },
            callback,
        )
    }
}
