// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct RecordRunFailureArgs {
    pub failure_id: String,
    pub run_id: String,
    pub lease_id: String,
    pub session_id: String,
    pub failure_code: String,
    pub failure_message: String,
    pub failure_type: String,
    pub retryable: bool,
    pub details_json: String,
}

impl From<RecordRunFailureArgs> for super::Reducer {
    fn from(args: RecordRunFailureArgs) -> Self {
        Self::RecordRunFailure {
            failure_id: args.failure_id,
            run_id: args.run_id,
            lease_id: args.lease_id,
            session_id: args.session_id,
            failure_code: args.failure_code,
            failure_message: args.failure_message,
            failure_type: args.failure_type,
            retryable: args.retryable,
            details_json: args.details_json,
        }
    }
}

impl __sdk::InModule for RecordRunFailureArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `record_run_failure`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait record_run_failure {
    /// Request that the remote module invoke the reducer `record_run_failure` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`record_run_failure:record_run_failure_then`] to run a callback after the reducer completes.
    fn record_run_failure(
        &self,
        failure_id: String,
        run_id: String,
        lease_id: String,
        session_id: String,
        failure_code: String,
        failure_message: String,
        failure_type: String,
        retryable: bool,
        details_json: String,
    ) -> __sdk::Result<()> {
        self.record_run_failure_then(
            failure_id,
            run_id,
            lease_id,
            session_id,
            failure_code,
            failure_message,
            failure_type,
            retryable,
            details_json,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `record_run_failure` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn record_run_failure_then(
        &self,
        failure_id: String,
        run_id: String,
        lease_id: String,
        session_id: String,
        failure_code: String,
        failure_message: String,
        failure_type: String,
        retryable: bool,
        details_json: String,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl record_run_failure for super::RemoteReducers {
    fn record_run_failure_then(
        &self,
        failure_id: String,
        run_id: String,
        lease_id: String,
        session_id: String,
        failure_code: String,
        failure_message: String,
        failure_type: String,
        retryable: bool,
        details_json: String,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            RecordRunFailureArgs {
                failure_id,
                run_id,
                lease_id,
                session_id,
                failure_code,
                failure_message,
                failure_type,
                retryable,
                details_json,
            },
            callback,
        )
    }
}
