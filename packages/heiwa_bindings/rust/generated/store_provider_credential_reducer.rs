// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct StoreProviderCredentialArgs {
    pub credential_id: String,
    pub user_id: String,
    pub provider_id: String,
    pub credential_kind: String,
    pub credential_enc: String,
    pub rate_group: String,
    pub display_label: Option<String>,
}

impl From<StoreProviderCredentialArgs> for super::Reducer {
    fn from(args: StoreProviderCredentialArgs) -> Self {
        Self::StoreProviderCredential {
            credential_id: args.credential_id,
            user_id: args.user_id,
            provider_id: args.provider_id,
            credential_kind: args.credential_kind,
            credential_enc: args.credential_enc,
            rate_group: args.rate_group,
            display_label: args.display_label,
        }
    }
}

impl __sdk::InModule for StoreProviderCredentialArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `store_provider_credential`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait store_provider_credential {
    /// Request that the remote module invoke the reducer `store_provider_credential` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`store_provider_credential:store_provider_credential_then`] to run a callback after the reducer completes.
    fn store_provider_credential(
        &self,
        credential_id: String,
        user_id: String,
        provider_id: String,
        credential_kind: String,
        credential_enc: String,
        rate_group: String,
        display_label: Option<String>,
    ) -> __sdk::Result<()> {
        self.store_provider_credential_then(
            credential_id,
            user_id,
            provider_id,
            credential_kind,
            credential_enc,
            rate_group,
            display_label,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `store_provider_credential` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn store_provider_credential_then(
        &self,
        credential_id: String,
        user_id: String,
        provider_id: String,
        credential_kind: String,
        credential_enc: String,
        rate_group: String,
        display_label: Option<String>,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl store_provider_credential for super::RemoteReducers {
    fn store_provider_credential_then(
        &self,
        credential_id: String,
        user_id: String,
        provider_id: String,
        credential_kind: String,
        credential_enc: String,
        rate_group: String,
        display_label: Option<String>,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            StoreProviderCredentialArgs {
                credential_id,
                user_id,
                provider_id,
                credential_kind,
                credential_enc,
                rate_group,
                display_label,
            },
            callback,
        )
    }
}
