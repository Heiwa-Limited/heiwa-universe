// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct ContradictBeliefArgs {
    pub belief_id: String,
    pub challenger_belief_id: Option<String>,
    pub challenger_source_id: Option<String>,
    pub contradiction_id: String,
    pub description: String,
    pub severity: f64,
}

impl From<ContradictBeliefArgs> for super::Reducer {
    fn from(args: ContradictBeliefArgs) -> Self {
        Self::ContradictBelief {
            belief_id: args.belief_id,
            challenger_belief_id: args.challenger_belief_id,
            challenger_source_id: args.challenger_source_id,
            contradiction_id: args.contradiction_id,
            description: args.description,
            severity: args.severity,
        }
    }
}

impl __sdk::InModule for ContradictBeliefArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `contradict_belief`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait contradict_belief {
    /// Request that the remote module invoke the reducer `contradict_belief` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`contradict_belief:contradict_belief_then`] to run a callback after the reducer completes.
    fn contradict_belief(
        &self,
        belief_id: String,
        challenger_belief_id: Option<String>,
        challenger_source_id: Option<String>,
        contradiction_id: String,
        description: String,
        severity: f64,
    ) -> __sdk::Result<()> {
        self.contradict_belief_then(
            belief_id,
            challenger_belief_id,
            challenger_source_id,
            contradiction_id,
            description,
            severity,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `contradict_belief` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn contradict_belief_then(
        &self,
        belief_id: String,
        challenger_belief_id: Option<String>,
        challenger_source_id: Option<String>,
        contradiction_id: String,
        description: String,
        severity: f64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl contradict_belief for super::RemoteReducers {
    fn contradict_belief_then(
        &self,
        belief_id: String,
        challenger_belief_id: Option<String>,
        challenger_source_id: Option<String>,
        contradiction_id: String,
        description: String,
        severity: f64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            ContradictBeliefArgs {
                belief_id,
                challenger_belief_id,
                challenger_source_id,
                contradiction_id,
                description,
                severity,
            },
            callback,
        )
    }
}
