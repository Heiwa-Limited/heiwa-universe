// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct RevokeCapabilityChainArgs {
    pub lease_id: String,
    pub revoked_at: String,
    pub revocation_reason: Option<String>,
}

impl From<RevokeCapabilityChainArgs> for super::Reducer {
    fn from(args: RevokeCapabilityChainArgs) -> Self {
        Self::RevokeCapabilityChain {
            lease_id: args.lease_id,
            revoked_at: args.revoked_at,
            revocation_reason: args.revocation_reason,
        }
    }
}

impl __sdk::InModule for RevokeCapabilityChainArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `revoke_capability_chain`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait revoke_capability_chain {
    /// Request that the remote module invoke the reducer `revoke_capability_chain` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`revoke_capability_chain:revoke_capability_chain_then`] to run a callback after the reducer completes.
    fn revoke_capability_chain(
        &self,
        lease_id: String,
        revoked_at: String,
        revocation_reason: Option<String>,
    ) -> __sdk::Result<()> {
        self.revoke_capability_chain_then(lease_id, revoked_at, revocation_reason, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `revoke_capability_chain` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn revoke_capability_chain_then(
        &self,
        lease_id: String,
        revoked_at: String,
        revocation_reason: Option<String>,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl revoke_capability_chain for super::RemoteReducers {
    fn revoke_capability_chain_then(
        &self,
        lease_id: String,
        revoked_at: String,
        revocation_reason: Option<String>,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            RevokeCapabilityChainArgs {
                lease_id,
                revoked_at,
                revocation_reason,
            },
            callback,
        )
    }
}
