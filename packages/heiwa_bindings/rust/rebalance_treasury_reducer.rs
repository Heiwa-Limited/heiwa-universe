// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct RebalanceTreasuryArgs {
    pub treasury_id: String,
}

impl From<RebalanceTreasuryArgs> for super::Reducer {
    fn from(args: RebalanceTreasuryArgs) -> Self {
        Self::RebalanceTreasury {
            treasury_id: args.treasury_id,
        }
    }
}

impl __sdk::InModule for RebalanceTreasuryArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `rebalance_treasury`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait rebalance_treasury {
    /// Request that the remote module invoke the reducer `rebalance_treasury` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`rebalance_treasury:rebalance_treasury_then`] to run a callback after the reducer completes.
    fn rebalance_treasury(&self, treasury_id: String) -> __sdk::Result<()> {
        self.rebalance_treasury_then(treasury_id, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `rebalance_treasury` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn rebalance_treasury_then(
        &self,
        treasury_id: String,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl rebalance_treasury for super::RemoteReducers {
    fn rebalance_treasury_then(
        &self,
        treasury_id: String,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp
            .invoke_reducer_with_callback(RebalanceTreasuryArgs { treasury_id }, callback)
    }
}
