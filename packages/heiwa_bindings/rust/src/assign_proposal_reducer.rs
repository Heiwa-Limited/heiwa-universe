// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct AssignProposalArgs {
    pub proposal_id: String,
    pub assigned_node_id: String,
    pub assignment_expires_at: String,
    pub hub_signature: String,
    pub proposal_hash: String,
    pub attempt_count: i64,
    pub eligibility_snapshot: Option<String>,
}

impl From<AssignProposalArgs> for super::Reducer {
    fn from(args: AssignProposalArgs) -> Self {
        Self::AssignProposal {
            proposal_id: args.proposal_id,
            assigned_node_id: args.assigned_node_id,
            assignment_expires_at: args.assignment_expires_at,
            hub_signature: args.hub_signature,
            proposal_hash: args.proposal_hash,
            attempt_count: args.attempt_count,
            eligibility_snapshot: args.eligibility_snapshot,
        }
    }
}

impl __sdk::InModule for AssignProposalArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `assign_proposal`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait assign_proposal {
    /// Request that the remote module invoke the reducer `assign_proposal` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`assign_proposal:assign_proposal_then`] to run a callback after the reducer completes.
    fn assign_proposal(
        &self,
        proposal_id: String,
        assigned_node_id: String,
        assignment_expires_at: String,
        hub_signature: String,
        proposal_hash: String,
        attempt_count: i64,
        eligibility_snapshot: Option<String>,
    ) -> __sdk::Result<()> {
        self.assign_proposal_then(
            proposal_id,
            assigned_node_id,
            assignment_expires_at,
            hub_signature,
            proposal_hash,
            attempt_count,
            eligibility_snapshot,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `assign_proposal` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn assign_proposal_then(
        &self,
        proposal_id: String,
        assigned_node_id: String,
        assignment_expires_at: String,
        hub_signature: String,
        proposal_hash: String,
        attempt_count: i64,
        eligibility_snapshot: Option<String>,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl assign_proposal for super::RemoteReducers {
    fn assign_proposal_then(
        &self,
        proposal_id: String,
        assigned_node_id: String,
        assignment_expires_at: String,
        hub_signature: String,
        proposal_hash: String,
        attempt_count: i64,
        eligibility_snapshot: Option<String>,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            AssignProposalArgs {
                proposal_id,
                assigned_node_id,
                assignment_expires_at,
                hub_signature,
                proposal_hash,
                attempt_count,
                eligibility_snapshot,
            },
            callback,
        )
    }
}
