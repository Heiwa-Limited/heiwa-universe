// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct InsertKnowledgeEmbeddingArgs {
    pub source_type: String,
    pub source_id: String,
    pub content_hash: String,
    pub embedding_json: String,
    pub ttl_hours: u32,
}

impl From<InsertKnowledgeEmbeddingArgs> for super::Reducer {
    fn from(args: InsertKnowledgeEmbeddingArgs) -> Self {
        Self::InsertKnowledgeEmbedding {
            source_type: args.source_type,
            source_id: args.source_id,
            content_hash: args.content_hash,
            embedding_json: args.embedding_json,
            ttl_hours: args.ttl_hours,
        }
    }
}

impl __sdk::InModule for InsertKnowledgeEmbeddingArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `insert_knowledge_embedding`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait insert_knowledge_embedding {
    /// Request that the remote module invoke the reducer `insert_knowledge_embedding` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`insert_knowledge_embedding:insert_knowledge_embedding_then`] to run a callback after the reducer completes.
    fn insert_knowledge_embedding(
        &self,
        source_type: String,
        source_id: String,
        content_hash: String,
        embedding_json: String,
        ttl_hours: u32,
    ) -> __sdk::Result<()> {
        self.insert_knowledge_embedding_then(
            source_type,
            source_id,
            content_hash,
            embedding_json,
            ttl_hours,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `insert_knowledge_embedding` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn insert_knowledge_embedding_then(
        &self,
        source_type: String,
        source_id: String,
        content_hash: String,
        embedding_json: String,
        ttl_hours: u32,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl insert_knowledge_embedding for super::RemoteReducers {
    fn insert_knowledge_embedding_then(
        &self,
        source_type: String,
        source_id: String,
        content_hash: String,
        embedding_json: String,
        ttl_hours: u32,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            InsertKnowledgeEmbeddingArgs {
                source_type,
                source_id,
                content_hash,
                embedding_json,
                ttl_hours,
            },
            callback,
        )
    }
}
