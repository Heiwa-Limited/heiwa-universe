// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct ResumeMissionArgs {
    pub mission_id: String,
    pub updated_at: String,
    pub summary: Option<String>,
}

impl From<ResumeMissionArgs> for super::Reducer {
    fn from(args: ResumeMissionArgs) -> Self {
        Self::ResumeMission {
            mission_id: args.mission_id,
            updated_at: args.updated_at,
            summary: args.summary,
        }
    }
}

impl __sdk::InModule for ResumeMissionArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `resume_mission`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait resume_mission {
    /// Request that the remote module invoke the reducer `resume_mission` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`resume_mission:resume_mission_then`] to run a callback after the reducer completes.
    fn resume_mission(
        &self,
        mission_id: String,
        updated_at: String,
        summary: Option<String>,
    ) -> __sdk::Result<()> {
        self.resume_mission_then(mission_id, updated_at, summary, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `resume_mission` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn resume_mission_then(
        &self,
        mission_id: String,
        updated_at: String,
        summary: Option<String>,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl resume_mission for super::RemoteReducers {
    fn resume_mission_then(
        &self,
        mission_id: String,
        updated_at: String,
        summary: Option<String>,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            ResumeMissionArgs {
                mission_id,
                updated_at,
                summary,
            },
            callback,
        )
    }
}
