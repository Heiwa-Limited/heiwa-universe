// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct ClaimProposalArgs {
    pub proposal_id: String,
    pub node_id: String,
    pub claimed_at: String,
    pub lease_expires_at: String,
}

impl From<ClaimProposalArgs> for super::Reducer {
    fn from(args: ClaimProposalArgs) -> Self {
        Self::ClaimProposal {
            proposal_id: args.proposal_id,
            node_id: args.node_id,
            claimed_at: args.claimed_at,
            lease_expires_at: args.lease_expires_at,
        }
    }
}

impl __sdk::InModule for ClaimProposalArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `claim_proposal`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait claim_proposal {
    /// Request that the remote module invoke the reducer `claim_proposal` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`claim_proposal:claim_proposal_then`] to run a callback after the reducer completes.
    fn claim_proposal(
        &self,
        proposal_id: String,
        node_id: String,
        claimed_at: String,
        lease_expires_at: String,
    ) -> __sdk::Result<()> {
        self.claim_proposal_then(
            proposal_id,
            node_id,
            claimed_at,
            lease_expires_at,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `claim_proposal` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn claim_proposal_then(
        &self,
        proposal_id: String,
        node_id: String,
        claimed_at: String,
        lease_expires_at: String,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl claim_proposal for super::RemoteReducers {
    fn claim_proposal_then(
        &self,
        proposal_id: String,
        node_id: String,
        claimed_at: String,
        lease_expires_at: String,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            ClaimProposalArgs {
                proposal_id,
                node_id,
                claimed_at,
                lease_expires_at,
            },
            callback,
        )
    }
}
