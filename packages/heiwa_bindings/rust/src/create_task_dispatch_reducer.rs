// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct CreateTaskDispatchArgs {
    pub task_id: String,
    pub parent_task_id: Option<String>,
    pub intent_class: String,
    pub risk_level: String,
    pub assigned_model: String,
    pub effort_knob: String,
    pub assigned_cell: String,
    pub budget_max_turns: u8,
    pub budget_max_seconds: u32,
    pub fallback_model: String,
    pub sandbox_mode: String,
    pub tools_allowed_json: String,
    pub context_files_json: String,
}

impl From<CreateTaskDispatchArgs> for super::Reducer {
    fn from(args: CreateTaskDispatchArgs) -> Self {
        Self::CreateTaskDispatch {
            task_id: args.task_id,
            parent_task_id: args.parent_task_id,
            intent_class: args.intent_class,
            risk_level: args.risk_level,
            assigned_model: args.assigned_model,
            effort_knob: args.effort_knob,
            assigned_cell: args.assigned_cell,
            budget_max_turns: args.budget_max_turns,
            budget_max_seconds: args.budget_max_seconds,
            fallback_model: args.fallback_model,
            sandbox_mode: args.sandbox_mode,
            tools_allowed_json: args.tools_allowed_json,
            context_files_json: args.context_files_json,
        }
    }
}

impl __sdk::InModule for CreateTaskDispatchArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `create_task_dispatch`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait create_task_dispatch {
    /// Request that the remote module invoke the reducer `create_task_dispatch` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`create_task_dispatch:create_task_dispatch_then`] to run a callback after the reducer completes.
    fn create_task_dispatch(
        &self,
        task_id: String,
        parent_task_id: Option<String>,
        intent_class: String,
        risk_level: String,
        assigned_model: String,
        effort_knob: String,
        assigned_cell: String,
        budget_max_turns: u8,
        budget_max_seconds: u32,
        fallback_model: String,
        sandbox_mode: String,
        tools_allowed_json: String,
        context_files_json: String,
    ) -> __sdk::Result<()> {
        self.create_task_dispatch_then(
            task_id,
            parent_task_id,
            intent_class,
            risk_level,
            assigned_model,
            effort_knob,
            assigned_cell,
            budget_max_turns,
            budget_max_seconds,
            fallback_model,
            sandbox_mode,
            tools_allowed_json,
            context_files_json,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `create_task_dispatch` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn create_task_dispatch_then(
        &self,
        task_id: String,
        parent_task_id: Option<String>,
        intent_class: String,
        risk_level: String,
        assigned_model: String,
        effort_knob: String,
        assigned_cell: String,
        budget_max_turns: u8,
        budget_max_seconds: u32,
        fallback_model: String,
        sandbox_mode: String,
        tools_allowed_json: String,
        context_files_json: String,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl create_task_dispatch for super::RemoteReducers {
    fn create_task_dispatch_then(
        &self,
        task_id: String,
        parent_task_id: Option<String>,
        intent_class: String,
        risk_level: String,
        assigned_model: String,
        effort_knob: String,
        assigned_cell: String,
        budget_max_turns: u8,
        budget_max_seconds: u32,
        fallback_model: String,
        sandbox_mode: String,
        tools_allowed_json: String,
        context_files_json: String,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            CreateTaskDispatchArgs {
                task_id,
                parent_task_id,
                intent_class,
                risk_level,
                assigned_model,
                effort_knob,
                assigned_cell,
                budget_max_turns,
                budget_max_seconds,
                fallback_model,
                sandbox_mode,
                tools_allowed_json,
                context_files_json,
            },
            callback,
        )
    }
}
