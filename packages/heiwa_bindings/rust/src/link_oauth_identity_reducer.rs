// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct LinkOauthIdentityArgs {
    pub identity_id: String,
    pub user_id: String,
    pub provider: String,
    pub provider_user_id: String,
    pub username: String,
    pub access_token_enc: String,
    pub refresh_token_enc: Option<String>,
    pub token_expires_at: Option<String>,
    pub scopes_json: String,
}

impl From<LinkOauthIdentityArgs> for super::Reducer {
    fn from(args: LinkOauthIdentityArgs) -> Self {
        Self::LinkOauthIdentity {
            identity_id: args.identity_id,
            user_id: args.user_id,
            provider: args.provider,
            provider_user_id: args.provider_user_id,
            username: args.username,
            access_token_enc: args.access_token_enc,
            refresh_token_enc: args.refresh_token_enc,
            token_expires_at: args.token_expires_at,
            scopes_json: args.scopes_json,
        }
    }
}

impl __sdk::InModule for LinkOauthIdentityArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `link_oauth_identity`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait link_oauth_identity {
    /// Request that the remote module invoke the reducer `link_oauth_identity` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`link_oauth_identity:link_oauth_identity_then`] to run a callback after the reducer completes.
    fn link_oauth_identity(
        &self,
        identity_id: String,
        user_id: String,
        provider: String,
        provider_user_id: String,
        username: String,
        access_token_enc: String,
        refresh_token_enc: Option<String>,
        token_expires_at: Option<String>,
        scopes_json: String,
    ) -> __sdk::Result<()> {
        self.link_oauth_identity_then(
            identity_id,
            user_id,
            provider,
            provider_user_id,
            username,
            access_token_enc,
            refresh_token_enc,
            token_expires_at,
            scopes_json,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `link_oauth_identity` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn link_oauth_identity_then(
        &self,
        identity_id: String,
        user_id: String,
        provider: String,
        provider_user_id: String,
        username: String,
        access_token_enc: String,
        refresh_token_enc: Option<String>,
        token_expires_at: Option<String>,
        scopes_json: String,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl link_oauth_identity for super::RemoteReducers {
    fn link_oauth_identity_then(
        &self,
        identity_id: String,
        user_id: String,
        provider: String,
        provider_user_id: String,
        username: String,
        access_token_enc: String,
        refresh_token_enc: Option<String>,
        token_expires_at: Option<String>,
        scopes_json: String,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            LinkOauthIdentityArgs {
                identity_id,
                user_id,
                provider,
                provider_user_id,
                username,
                access_token_enc,
                refresh_token_enc,
                token_expires_at,
                scopes_json,
            },
            callback,
        )
    }
}
