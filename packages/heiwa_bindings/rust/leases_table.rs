// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use super::lease_type::Lease;
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

/// Table handle for the table `leases`.
///
/// Obtain a handle from the [`LeasesTableAccess::leases`] method on [`super::RemoteTables`],
/// like `ctx.db.leases()`.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.leases().on_insert(...)`.
pub struct LeasesTableHandle<'ctx> {
    imp: __sdk::TableHandle<Lease>,
    ctx: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the table `leases`.
///
/// Implemented for [`super::RemoteTables`].
pub trait LeasesTableAccess {
    #[allow(non_snake_case)]
    /// Obtain a [`LeasesTableHandle`], which mediates access to the table `leases`.
    fn leases(&self) -> LeasesTableHandle<'_>;
}

impl LeasesTableAccess for super::RemoteTables {
    fn leases(&self) -> LeasesTableHandle<'_> {
        LeasesTableHandle {
            imp: self.imp.get_table::<Lease>("leases"),
            ctx: std::marker::PhantomData,
        }
    }
}

pub struct LeasesInsertCallbackId(__sdk::CallbackId);
pub struct LeasesDeleteCallbackId(__sdk::CallbackId);

impl<'ctx> __sdk::Table for LeasesTableHandle<'ctx> {
    type Row = Lease;
    type EventContext = super::EventContext;

    fn count(&self) -> u64 {
        self.imp.count()
    }
    fn iter(&self) -> impl Iterator<Item = Lease> + '_ {
        self.imp.iter()
    }

    type InsertCallbackId = LeasesInsertCallbackId;

    fn on_insert(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> LeasesInsertCallbackId {
        LeasesInsertCallbackId(self.imp.on_insert(Box::new(callback)))
    }

    fn remove_on_insert(&self, callback: LeasesInsertCallbackId) {
        self.imp.remove_on_insert(callback.0)
    }

    type DeleteCallbackId = LeasesDeleteCallbackId;

    fn on_delete(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> LeasesDeleteCallbackId {
        LeasesDeleteCallbackId(self.imp.on_delete(Box::new(callback)))
    }

    fn remove_on_delete(&self, callback: LeasesDeleteCallbackId) {
        self.imp.remove_on_delete(callback.0)
    }
}

pub struct LeasesUpdateCallbackId(__sdk::CallbackId);

impl<'ctx> __sdk::TableWithPrimaryKey for LeasesTableHandle<'ctx> {
    type UpdateCallbackId = LeasesUpdateCallbackId;

    fn on_update(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row, &Self::Row) + Send + 'static,
    ) -> LeasesUpdateCallbackId {
        LeasesUpdateCallbackId(self.imp.on_update(Box::new(callback)))
    }

    fn remove_on_update(&self, callback: LeasesUpdateCallbackId) {
        self.imp.remove_on_update(callback.0)
    }
}

/// Access to the `lease_id` unique index on the table `leases`,
/// which allows point queries on the field of the same name
/// via the [`LeasesLeaseIdUnique::find`] method.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.leases().lease_id().find(...)`.
pub struct LeasesLeaseIdUnique<'ctx> {
    imp: __sdk::UniqueConstraintHandle<Lease, String>,
    phantom: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

impl<'ctx> LeasesTableHandle<'ctx> {
    /// Get a handle on the `lease_id` unique index on the table `leases`.
    pub fn lease_id(&self) -> LeasesLeaseIdUnique<'ctx> {
        LeasesLeaseIdUnique {
            imp: self.imp.get_unique_constraint::<String>("lease_id"),
            phantom: std::marker::PhantomData,
        }
    }
}

impl<'ctx> LeasesLeaseIdUnique<'ctx> {
    /// Find the subscribed row whose `lease_id` column value is equal to `col_val`,
    /// if such a row is present in the client cache.
    pub fn find(&self, col_val: &String) -> Option<Lease> {
        self.imp.find(col_val)
    }
}

#[doc(hidden)]
pub(super) fn register_table(client_cache: &mut __sdk::ClientCache<super::RemoteModule>) {
    let _table = client_cache.get_or_make_table::<Lease>("leases");
    _table.add_unique_constraint::<String>("lease_id", |row| &row.lease_id);
}

#[doc(hidden)]
pub(super) fn parse_table_update(
    raw_updates: __ws::v2::TableUpdate,
) -> __sdk::Result<__sdk::TableUpdate<Lease>> {
    __sdk::TableUpdate::parse_table_update(raw_updates).map_err(|e| {
        __sdk::InternalError::failed_parse("TableUpdate<Lease>", "TableUpdate")
            .with_cause(e)
            .into()
    })
}

#[allow(non_camel_case_types)]
/// Extension trait for query builder access to the table `Lease`.
///
/// Implemented for [`__sdk::QueryTableAccessor`].
pub trait leasesQueryTableAccess {
    #[allow(non_snake_case)]
    /// Get a query builder for the table `Lease`.
    fn leases(&self) -> __sdk::__query_builder::Table<Lease>;
}

impl leasesQueryTableAccess for __sdk::QueryTableAccessor {
    fn leases(&self) -> __sdk::__query_builder::Table<Lease> {
        __sdk::__query_builder::Table::new("leases")
    }
}
