// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct UpsertDiscordUserArgs {
    pub user_id: u64,
    pub username: String,
    pub trust_score: f32,
}

impl From<UpsertDiscordUserArgs> for super::Reducer {
    fn from(args: UpsertDiscordUserArgs) -> Self {
        Self::UpsertDiscordUser {
            user_id: args.user_id,
            username: args.username,
            trust_score: args.trust_score,
        }
    }
}

impl __sdk::InModule for UpsertDiscordUserArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `upsert_discord_user`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait upsert_discord_user {
    /// Request that the remote module invoke the reducer `upsert_discord_user` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`upsert_discord_user:upsert_discord_user_then`] to run a callback after the reducer completes.
    fn upsert_discord_user(
        &self,
        user_id: u64,
        username: String,
        trust_score: f32,
    ) -> __sdk::Result<()> {
        self.upsert_discord_user_then(user_id, username, trust_score, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `upsert_discord_user` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn upsert_discord_user_then(
        &self,
        user_id: u64,
        username: String,
        trust_score: f32,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl upsert_discord_user for super::RemoteReducers {
    fn upsert_discord_user_then(
        &self,
        user_id: u64,
        username: String,
        trust_score: f32,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            UpsertDiscordUserArgs {
                user_id,
                username,
                trust_score,
            },
            callback,
        )
    }
}
