# AGENTS.md

Agent architecture and SpacetimeDB rules live in:

- `apps/heiwa_hub/agents/` for runtime agent code (Spine, Broker, Executor, Messenger)
- `apps/heiwa_hub/heiwaproductiondb/AGENTS.md` for SpacetimeDB SDK rules
- `config/identities/profiles.json` for the HeiwaCells agent catalog
